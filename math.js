function add(x, y) {
    return x + y;
}

function subtract(x, y) {
    return x - y;
}

function square(x) {
    return x * x;
}

module.exports = {
    add: add,
    subtract: subtract,
    square: square
}